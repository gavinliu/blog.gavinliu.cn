---
title: Stable Diffusion 基础 3 | 提示词快速上手
date: 2023-08-17 15:21:54
tags:
  - Stable Diffusion
  - Stable Diffusion 基础
categories:
  - AIGC
---

## 什么是提示词

讲提示词前，需要给大家先讲一个概念 `多模态`，所谓多模态其实就是是指多种数据类型，包括文本、图像、音视频、3D 物体等。

多模态交互是人工智能领域的研究热点之一，对于 AI 理解人类世界，帮助人类处理多种事务具有重要意义，所以多模态开始逐渐演变成基于文本和其他模态的交互，比如 ChatGPT 通过文本提示来生成文本、Stable Diffusion 通过文本提示来生成图片。

在这个大背景下，就产生了提示词这个概念，所谓提示就是告诉 AI 应该如何操作，为了更好的和 AI 进行交流，也产生了提示词工程这门专业技术。

同时针对不同模型也会有不同的提示词规则，比如 ChatGPT、 Stable Diffusion 提示词规则不能复用。

## SD 提示词语法

### 基本规则

- 提示词之间使用半角的逗号 `,` 分割
- 越靠前的提示词优先级越高

### 权重规则

- 默认权重为 1
- `(提示词)` 表示 1.1 倍权重
- `{提示词}` 表示 1.05 倍权重
- `[提示词]` 表示 0.9 倍权重
- 使用 `(提示词:0-1数值)` 直接设置权重

### LoRA 规则

- 使用 `<>` 来调用 LoRA
- `<lora:文件名:权重> 触发词`

### 组合词规则

- 使用 `_` 来表示一个组合词
- 比如
  - 巧克力 牛奶，模型会认为是两个物品
  - 巧克力\_牛奶，这样可以让模型认为是一个物品

### 提示词生效控制

图片生成是通过不断采样，下面的方式可以设置在特定的采样步数提示词才开始生效

- 使用 `[提示词:0-1数值]` 设置提示词在 数值\*总步数 时开始生效
- 使用 `[提示词::0-1数值]` 设置提示词在 数值\*总步数 时结束生效
- 使用 `[提示词1:提示词2:0-1数值]` 设置提示词 1 生效时机从 0 开始到 数值\*总步数 结束，之后提示词 2 开始生效
- 使用 `[提示词1|提示词2]` 设置两个提示词交替生效

## SD 提示词模版

- 正向提示词（你想要什么）

  - 画面质量&风格

    ```
    masterpiece,
    ```

    ```
    杰作,
    ```

  - 主体描述

    ```
    1girl, round face, hime cut, kind_smile,
    ```

    ```
    一个女孩, 圆脸，公主切发型，开心的微笑
    ```

  - 背景描述

    ```
    laboratory,
    ```

    ```
    实验室
    ```

    - 由于场景描述比较靠后，可以适当增加权重

  - LoRA

- 反向提示词（你不想要什么）

```
NSFW, ugly, Multiple people, missing limb, bad hands, mutated hands and fingers, poorly drawn hands, extra legs,
```

```
不适合工作场景，丑陋，多人，缺少肢体，坏手，畸形的手和手指，画不好的手，多余的腿
```

---

```Prompt 正向提示词
masterpiece,
1girl, round face, hime cut, kind_smile,
(laboratory:1.5),
```

```Prompt 反向提示词
masterpiece,
1girl, round face, hime cut, kind_smile,
(laboratory:1.5),
```

## SD 提示词插件推荐

- https://github.com/Physton/sd-webui-prompt-all-in-one

---

好了，掌握了基本的语法 + 上提示词插件，算是入门
